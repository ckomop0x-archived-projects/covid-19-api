// /* eslint-disable no-param-reassign */
// module.exports = {
//   webpack: (config, { isServer }) => {
//     // Fixes npm packages that depend on `fs` module
//     if (!isServer) {
//       config.node = {
//         // fs: 'empty',
//         // dns: 'empty',
//         // net: 'empty',
//         // tls: 'empty',
//       };
//     }

//     return config;
//   },
// };
